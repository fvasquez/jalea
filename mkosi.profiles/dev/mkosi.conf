# Development profile: `mkosi --profile=dev build`, then `mkosi vm` and
# `mkosi ssh`. Adds an SSH-over-vsock socket and the dev key pair from
# scripts/gen-dev-keys.sh. Never used for release images.
[Content]
Ssh=yes

[Runtime]
SshKey=../../keys/ssh.key
SshCertificate=../../keys/ssh.crt
