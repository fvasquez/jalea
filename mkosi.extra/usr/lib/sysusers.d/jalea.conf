# Accounts Jalea declares itself. Everything else Debian created at build
# time comes from zz-jalea-factory.conf, generated by mkosi.finalize.

# tcpdump drops its privileges to this account once the capture is open
# (its -Z default), so a capture as root fails without it.
u tcpdump - "tcpdump packet capture" - -
