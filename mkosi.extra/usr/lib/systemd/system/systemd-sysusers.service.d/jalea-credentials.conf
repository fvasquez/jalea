# systemd 257's unit only imports password credentials for root. The installer
# hands over the first user's password as passwd.hashed-password.<user>, so
# import them for every user.
[Service]
ImportCredential=passwd.hashed-password.*
ImportCredential=passwd.plaintext-password.*
ImportCredential=passwd.shell.*
