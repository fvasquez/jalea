# Debian's unit starts /usr/bin/nix-daemon. Route through a wrapper that
# prefers the Nix installed by nix-upgrade.service.
[Service]
ExecStart=
ExecStart=@/usr/lib/jalea/nix-daemon nix-daemon --daemon
