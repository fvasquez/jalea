# Wait for any one interface to be online, not all of them: a NUC on Wi-Fi
# with the Ethernet port unplugged must not wait two minutes and then fail.
[Service]
ExecStart=
ExecStart=/lib/systemd/systemd-networkd-wait-online --any
