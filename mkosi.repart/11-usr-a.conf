# Slot group A: the operating system. Read-only erofs, verity protected. Its
# GPT UUID is the first half of the root hash.
[Partition]
Type=usr
Label=usr_a
Format=erofs
CopyFiles=/usr:/
Verity=data
VerityMatchKey=usr
Minimize=yes
Compression=zstd
SplitName=usr
